version: 2.1

#
# Code base from Lauren
#
# I have commented out Lauren's code and edited for build and test jobs to work
# You might want to uncomment the code in your section and see if it works for you
#

#ORBS:
orbs:
  docker: circleci/docker@2.1.4
  heroku: circleci/heroku@2.0.0
  snyk: snyk/snyk@1.5.0

#JOBS: build, test, scan, publish, pull-and-deploy
jobs:
  build:
    docker:
      - image: cimg/openjdk:17.0
      - image: cimg/postgres:16.1.0
    steps:
      - checkout
      - run:
          name: Build
          command: mvn -B -DskipTests clean package
  test:
    docker:
      - image: cimg/openjdk:17.0
      - image: cimg/postgres:16.1.0
    steps:
      - checkout
      - run:
          name: Test
          command: mvn test
  #sing?
  scan:
  # scan:
  #   docker:
  #     - image: cimg/node:16.10
  #   environment:
  #     IMAGE_NAME: #TBC Image Name (Eg: laurenong/education-space) [elly: is this from chatgpt? i think it's wrong, image name should be cimg/openjdk:17.0]
  #   steps:
  #     - checkout
  #     - setup_remote_docker
  #     - docker/check
  #     - run: docker build -t $IMAGE_NAME .
  #     - snyk/scan:
  #         docker-image-name: $IMAGE_NAME

  #anu's portion below
  publish:
    # executor: docker/docker
    # steps:
    #   - setup_remote_docker
    #   - checkout
    #   - docker/check
    #   - docker/build:
    #       image: anusuyadevi2024/group1module4project
    #       tag: latest
    #   - docker/push:
    #       image: anusuyadevi2024/group1module4project
    #       tag: latest
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - setup_remote_docker
      - checkout
      - run:
          name: publish Docker Image
          command: |
            docker build -t anusuyadevi2024/group1module4project:latest .
            echo "DOCKER_USERNAME: $DOCKER_USERNAME"
            echo "DOCKER_PASSWORD: $DOCKER_PASSWORD"
            echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
            docker push anusuyadevi2024/group1module4project:latest

#WORKFLOW: ci_flow = dev branch & cicd_flow = release branch
workflows:
  ci_flow:
    jobs:
      - build:
          filters:
            branches:
              only:
                - develop
      - test:
          filters:
            branches:
              only:
                - develop
          requires:
            - build

      #sing?
      #     - scan:
      #       filters:
      #         branches:
      #           only:
      #             - development
      #         requires:
      #             - test

  cicd_flow:
    jobs:
      #lauren's portion below
      - build:
          filters:
            branches:
              only:
                - develop
      - test:
          filters:
            branches:
              only:
                - develop
          requires:
            - build
      #           # + all tests pass

      #sing?
      #     - scan:
      #       filters:
      #         branches:
      #           only:
      #             - release
      #         requires:
      #             - test

      # anu's portion below
      - publish:
          filters:
            branches:
              only:
                - develop
              # requires:
              #   - scan

      #georgiana's portion below
      - heroku/push-docker-image:
          app-name: group1module4
          pre-deploy:
            - setup_remote_docker
          filters:
            branches:
              only:
                - develop
          requires:
            - publish
